import 'package:flutter/material.dart';

class Spark {
  Spark(this.x, this.y, this.dx, this.dy, this.life, this.color);

  double x;
  double y;
  double dx;
  double dy;
  double life;
  Color color;
}
